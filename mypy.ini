[mypy]
python_version = 3.10
ignore_missing_imports = True
warn_unused_configs = True
# Fatal in CI (REPO_CONTRACT.md 4b). No `|| true`. If a check cannot pass,
# fix the types or narrow deliberately rather than downgrading the gate.
follow_imports = silent
exclude = (build|dist|tests)/
